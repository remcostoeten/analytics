{
	"rewrites": [{ "source": "/(.*)", "destination": "/api" }],
	"buildCommand": "bun run --filter @remcostoeten/ingestion build && bun run scripts/build.ts",
	"installCommand": "bun install",
	"framework": null
}
