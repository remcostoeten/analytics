/**
 * A no-op (no operation) function.
 *
 * Commonly used as a placeholder or default
 * when a function is required but no behavior is needed.
 *
 * @example
 * ```ts
 * catch {
 *   noop();
 * }
 * ```
 */
export function noop(): void {}
